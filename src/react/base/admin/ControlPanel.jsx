import React from 'react';
import { Button } from 'reactstrap';

const ControlPanel = () => (
    <div>
        <Button
            onClick={() => {}}
            bsStyle="primary"
        >
            Upload data...
        </Button>
    </div>
);


export default ControlPanel;
